require "spec_helper"

describe Fastlane::Cop do
  it "has a version number" do
    expect(Fastlane::Cop::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
